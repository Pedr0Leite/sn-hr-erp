{
    "name": "sn-hr-erp",
    "version": "0.0.7",
    "description": "",
    "license": "UNLICENSED",
    "imports": {
        "#now:*": "./@types/servicenow/fluent/*/index.js"
    },
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "tosn": "npm version patch --no-git-tag-version && npm run build && npm run deploy",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies",
        "check": "node scripts/check-contract.mjs && node scripts/check-nv-logic.mjs && node scripts/check-minimisation.mjs && node scripts/check-store-readiness.mjs"
    },
    "dependencies": {
        "react": "18.2.0",
        "react-dom": "18.2.0",
        "@servicenow/react-components": "^0.1.0"
    },
    "devDependencies": {
        "@servicenow/sdk": "4.9.0",
        "@servicenow/glide": "27.0.5",
        "@types/react": "18.3.12",
        "@types/react-dom": "18.3.1"
    }
}
